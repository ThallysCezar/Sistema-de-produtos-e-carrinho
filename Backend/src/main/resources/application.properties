spring.application.name=desafio2

# Profile ativo (dev por padrao, prod no Docker)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# PostgreSQL - usando variaveis de ambiente com fallback
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/postgres}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:123456}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:drop-and-create}

# JPA, SQL
spring.jpa.properties.hibernate.format_sql=true

# RabbitMQ - usando variaveis de ambiente com fallback
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}